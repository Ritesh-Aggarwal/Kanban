export type Task = {
  name: string;
};

export type Board = {
  id: number;
  title: string;
  description: string;
};

export type Stage = {
  id: number;
  title: string;
  description: string;
};
